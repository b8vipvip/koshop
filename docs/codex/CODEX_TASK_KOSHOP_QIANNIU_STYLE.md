你现在负责二开我的 koshop 项目。

项目目录结构：

* `third_party/dujiao-next/api`：Dujiao-Next 后端 API，负责商品、订单、支付、钱包、自动发货等。
* `third_party/dujiao-next/user`：Dujiao-Next 买家前台。
* `third_party/dujiao-next/admin`：Dujiao-Next 管理后台。
* `third_party/live-helper-chat`：Live Helper Chat 客服系统。
* 线上测试域名：

  * 商城前台：`https://shop.cn12.vip`
  * 商城后台：`https://admin.cn12.vip`
  * 客服系统：`https://kefu.cn12.vip`

重要限制：

不要复制淘宝、千牛的商标、Logo、图标、素材、完全一致视觉设计。只参考电商客服和卖家工作台的交互方式，做一个原创 UI。

第一部分：改 Live Helper Chat。

目标：把 Live Helper Chat 改成更适合电商店铺客服的界面。

买家前台聊天界面要求：

1. 做成移动端优先的电商客服聊天界面。
2. 顶部有返回按钮、客服标题、在线/离线状态。
3. 中间是聊天消息列表：

   * 买家消息在右侧。
   * 客服消息在左侧。
   * 支持时间分隔。
   * 保留系统消息。
4. 底部是输入框、发送按钮。
5. 如果 Live Helper Chat 已支持图片、文件、表情、快捷入口，尽量保留。
6. 支持全页面聊天和小组件聊天。
7. 保持原有开始聊天、发送消息、客服回复、离线留言、部门分配、客服在线状态等功能不坏。

客服后台聊天界面要求：

1. 做一个类似电商卖家客服工作台的界面。
2. 左侧是买家咨询列表：

   * 显示访客 ID / 买家昵称。
   * 显示最后一条消息。
   * 显示未读数量。
   * 显示时间。
   * 显示在线/等待/已结束状态。
3. 点击某个买家后，中间区域显示聊天窗口。
4. 中间聊天窗口包含：

   * 买家和客服消息气泡。
   * 时间。
   * 输入框。
   * 发送按钮。
   * 快捷回复。
   * 图片/文件入口，如果原系统支持就保留。
   * 结束聊天、转接、标记完成，如果原系统支持就保留。
5. 右侧显示买家信息面板：

   * 访客 IP / 浏览页面 / 来源页面。
   * 后续预留订单信息、手机号、兑换码状态。
6. 一个客服账号要能同时接待多个买家。
7. 桌面端为三栏布局，移动端为列表页和聊天详情页切换。
8. 不要删除 Live Helper Chat 原后台，优先新增一个更适合电商客服的工作台页面。
9. 尽量用 Live Helper Chat 的模块、模板、主题方式实现，减少修改核心代码。如果必须改核心文件，请在结果中列出改了哪些文件。

第二部分：改 Dujiao-Next 买家前台。

目标 1：移动端二级以上页面隐藏底部导航。

当前移动端底部导航大概是这个结构：

`<nav class="lg:hidden fixed bottom-0 left-0 right-0 z-40 theme-panel-strong backdrop-blur-xl border-t theme-border theme-safe-bottom">...</nav>`

要求：

1. 顶级页面保留底部导航，例如：

   * `/`
   * `/products`
   * `/cart`
   * `/auth/login`
   * 个人中心首页，如果有独立首页
2. 二级以上页面隐藏底部导航，例如：

   * 商品详情页
   * 订单详情页
   * 结算页
   * 支付页
   * 博客详情页
   * 公告详情页
   * 充值订单详情页
   * 任何带 ID 或更深层级的页面
3. 隐藏底部导航的页面，左上角必须保留返回箭头按钮。
4. 如果页面已有返回按钮，保留即可。
5. 如果页面没有返回按钮，需要新增。
6. 返回逻辑：

   * 优先 `router.back()`
   * 如果没有历史记录，回到 `/`

目标 2：加入客服入口。

Live Helper Chat 地址：

* 全页面聊天地址：`https://kefu.cn12.vip/index.php/chat/start`
* 如果已有全局小组件，也可以调用小组件打开函数。

要求：

1. 在买家前台首页右上角加入客服图标。
2. 在个人中心页面右上角加入客服图标。
3. 点击客服图标打开客服聊天。
4. 在商品详情页，把客服入口放到购买按钮区域。

商品详情页当前购买按钮区域类似：

`<div class="grid grid-cols-1 sm:grid-cols-2 gap-4"><button>加入购物车</button><button>立即购买</button></div>`

改成同一行三个元素，顺序从左到右：

1. 客服图标 / 客服按钮
2. 加入购物车
3. 立即购买

手机端要求：

1. 三个元素必须在同一行。
2. 不要换行。
3. 客服按钮可以是小宽度图标按钮。
4. 加入购物车和立即购买按钮适当缩小 padding 和字体。
5. 保留原有禁用状态、库存判断、购买逻辑、购物车逻辑。

第三部分：额外开发统一卖家后台。

问题：能否额外开发一个管理员后台，只用于管理 Dujiao-Next 的商品、订单、交易、财务、Live Helper Chat 的客服接待，UI 和功能类似电商卖家客户端？

答案：可以。

请新增独立应用，不要强行替换原 Dujiao-Next Admin 和 Live Helper Chat Admin。

建议目录：

`apps/koshop-seller-console`

功能模块：

1. 工作台首页：

   * 今日订单数
   * 今日支付金额
   * 待处理订单
   * 低库存商品
   * 当前咨询人数
   * 当前待回复咨询
2. 商品管理：

   * 商品列表
   * 新增商品
   * 编辑商品
   * 上架/下架
   * 卡密库存概览
3. 订单管理：

   * 订单列表
   * 订单详情
   * 支付状态
   * 发货状态
   * 手动处理/补发/退款入口，如果 API 支持
4. 交易/财务：

   * 支付记录
   * 钱包充值记录
   * 退款记录
   * 收入、成本、利润统计，如果数据支持
5. 客服接待：

   * 集成 Live Helper Chat 的买家咨询列表。
   * 显示当前聊天。
   * 显示买家信息。
   * 后续预留根据手机号、订单号、兑换码查询订单状态。
6. 系统设置：

   * 站点设置快捷入口。
   * 支付配置快捷入口。
   * 客服配置快捷入口。

技术要求：

1. 优先使用 Dujiao-Next Admin API 获取商品、订单、支付、财务数据。
2. 优先使用 Live Helper Chat API、嵌入页面或模块方式集成客服。
3. 不要把数据库密码写到前端。
4. 不要把 API secret 写到前端。
5. 如果 Live Helper Chat 没有合适 API，可以第一阶段用 iframe 嵌入新的客服工作台页面，后续再做深度 API 对接。
6. 保留原 Dujiao-Next Admin 和 Live Helper Chat Admin 作为兜底后台。
7. 所有新增配置使用环境变量或独立配置文件。
8. 提供构建和运行说明。
9. 提供回滚说明。
10. 提供测试清单。

交付要求：

1. 列出修改过的所有文件。
2. 说明每个模块怎么运行。
3. 说明如何构建 Dujiao-Next user 前台。
4. 说明如何部署新的 Live Helper Chat 修改。
5. 说明如何部署新的统一卖家后台。
6. 给出测试步骤：

   * 买家打开商城首页看到客服入口。
   * 买家打开商品详情页看到客服、加入购物车、立即购买同一行。
   * 买家发起咨询。
   * 客服在新工作台看到买家列表。
   * 客服点买家进入聊天。
   * 客服回复消息。
   * 买家收到回复。
   * 二级页面移动端底部导航隐藏。
   * 二级页面左上角返回按钮可用。
